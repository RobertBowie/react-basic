import React from 'react';
import { Button } from 'react-bootstrap';

const Controls = ({}) => {
  return (
    <div>
      <h3>Controls</h3>
      <Button>Start</Button>
    </div>

  );
}

export default Controls;
